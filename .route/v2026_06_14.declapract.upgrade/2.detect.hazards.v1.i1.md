# 2.detect.hazards — iteration 1

## status

hazards detected. type check fails.

## hazards

### 1. test hazard: jest.integration.env.ts argument count

- **file**: `jest.integration.env.ts:105`
- **error**: `Expected 0-1 arguments, but got 2`
- **likely cause**: `test-fns` upgrade changed function signature
- **verify**: `rhx git.repo.test --what types`

### 2. test hazard: HelpfulError type mismatch

- **file**: `src/domain.operations/file/genArtifactGitFile.ts:55,101`
- **error**: `ArtifactAccessDeniedError` not assignable to `HelpfulError`
- **likely cause**: `helpful-errors` 1.5.3 → 1.7.3 changed generic signature
- **verify**: `rhx git.repo.test --what types`

### 3. test hazard: RefByUnique type mismatch

- **files**: `gitFileGet.test.ts:30`, `gitFileSet.test.ts:33`
- **error**: `RefByUnique` type shape changed
- **likely cause**: `domain-objects` 0.31.9 → 0.31.13 changed Ref types
- **verify**: `rhx git.repo.test --what types`

### 4. path hazard: logic/ → domain.operations/

- **files**: `src/logic/file/.rhachet/*` → `src/domain.operations/file/.rhachet/*`
- **risk**: imports may break
- **verify**: grep for `src/logic/file` (checked: no refs left)

## cicd hazards

none detected — workflow changes appear compatible.

## next steps

proceed to stone 3.1 to repair test defects.
